# 

> LiveBOS Studio > 概念手册 > 基础架构

---

## 系统变量

### 1.1.1.全局变量

变量的生命周期为全局，即变量被赋值后，该值一直有效，即使服务器被重启。

### 1.1.2.用户变量

为每个用户绑定一个变量，在当前的用户变量被赋值后，不会影响其他的用户变量。其生命周期也为全局。

### 1.1.3.会话期变量

变量的生命周期为会话期，即变量赋值变更只会影响当前的会话期。

### 1.1.4.cookie变量

存放在浏览器客户端的变量。

### 1.1.5.线程期变量

变量的生命周期为当前线程
