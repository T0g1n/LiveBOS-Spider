# 

> LiveBOS Studio > 概念手册 > 对象模型 > 对象类型

---

### 虚拟对象

虚拟对象本身在数据库中并不存在，也不能被其他对象通过内部对象方式关联。它是在数据记录集基础上建立的对象，拥有和实体对象相同的属性，但字段必须映射到对应的记录集中的字段，并且可以设计自定义的用户方法。

#### 支持动态扩展列

可以将数据集中其他未映射的字段作为该虚拟对象的扩展字段。注意，这些扩展的动态列只支持列表查看，不能作为操作参数等使用。
