# 

> LiveBOS Studio > 概念手册 > 对象模型 > 关联定义

---

### 外部对象关联

该对象与主对象没有直接关联关系，如：没有内部字段引用主对象。

#### 定制条件从属关联

一个带参数的视图或者查询可以通过将该对象的参数与主对象字段进行绑定，可实现从属关系。

#### 其他

##### 自由关联

该关联与主对象没有任何联系，只是以链接方式展示在主对象上，作为页面导航使用。

##### 需选中记录

与主对象的关系仅限于需要引用一个主对象记录才能导航到该页面上一般适用于自定义的JSP中。
