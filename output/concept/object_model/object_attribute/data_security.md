# 

> LiveBOS Studio > 概念手册 > 对象模型 > 对象属性

---

### 数据安全控制

#### 要求安全保护

必须通过SSL安全连接才能访问对象数据。

#### 要求安全审计

对对象的增、删、改操作记录审计日志中。

#### 删除控制模式

##### 自由删除

删除时不检查关联对象。

##### 同步删除关联对象

删除实例的同时，删除引用该对象实例的其他相应对象。

##### 有关联对象禁止删除

当对象实例被其他对象引用时,该实例禁止删除。

#### 允许WebService调用

是否允许WebService调用。
