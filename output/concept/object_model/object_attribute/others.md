# 

> LiveBOS Studio > 概念手册 > 对象模型 > 对象属性

---

### 其他

#### 作为Portlet使用

##### Portlet缓存

###### 是否允许缓存

在Portlet显示是否允许全局缓存数据。一般情况下，实体对象都可以进行缓存，但是与用户有关系的视图数据或者实体对象有数据范围限制时就不应进行缓存。

###### 缓存过期时间

由用户定义的缓存过期时间，若无配置则过期时间由系统参数决定。
