# 

> LiveBOS Studio > 概念手册 > 对象模型 > 对象属性 > 展现属性

---

#### 数据查找属性

##### 基本分组

展示界面时可以用分组字段的值快速筛选记录。

##### 层次分组

按设定的字段顺序树状展示分组（在界面方案为树状分组；值为5）中有效。

##### 细分对象作为分组项

展示界面用细分对象快速筛选记录。

##### 可搜索列

设定可显示在搜索栏的字段,默认为设置时为该对象全部可见字段。
